/*
 Constants for defining how lazy load behaves
 */

class LazyLoadSettings {

    static OFF_SCREEN_HEIGHT_TO_LOAD = 1000;
    // Pixel height of articles until I find a better way
    static HEIGHT_OF_ARTICLES = 10;

}

export default LazyLoadSettings;
